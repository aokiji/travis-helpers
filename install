#!/bin/bash
VERSION='v1.1'
mkdir -p /tmp/bin
curl -L https://github.com/nico-acidtango/travis-helpers/releases/download/$VERSION/travis-helpers > /tmp/bin/travis-helpers
chmod a+x /tmp/bin/travis-helpers
echo 'export PATH=$PATH:/tmp/bin'
